syntax = "proto3";

option csharp_namespace = "Grpc.Example";

package chat;

service ChatService {
  rpc Chat (stream ChatMessage) returns (stream ChatMessage);
}

message ChatMessage {
  string user = 1;
  string content = 2;
  string timestamp = 3;
}